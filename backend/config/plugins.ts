// backend/config/plugins.ts
export default () => ({
  upload: {
    // si querés customizar, podés dejarlo vacío o con config
    // config: { provider: 'local', providerOptions: { sizeLimit: 10 * 1024 * 1024 } }
  },
});
